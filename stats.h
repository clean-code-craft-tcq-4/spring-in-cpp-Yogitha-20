#include <vector>

namespace Statistics 
{
    Stats ComputeStatistics(const std::vector<___>& );

    class EmailAlert
    {
        void emailSent();
    };
    class LEDAlert
    {
        void ledGlows();   
    };
    class StatsAlerter
    {
        StatsAlerter();
        StatsAlerter(const float maxthreshold,std::vector< > alerter);
        void checkandalert(vector<float> vec);
    
    };
}
